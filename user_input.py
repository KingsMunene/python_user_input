#Ask the user for a name using the input method
name = input("Whats your name: ")

#Ask the user to input their age using the input method
age = input("What is your age: ")

#Ask the user for location using the input method
location = input("Where do you come from?: ")
print("Hello " + name + ", you are " + age + " years old and live in " + location)